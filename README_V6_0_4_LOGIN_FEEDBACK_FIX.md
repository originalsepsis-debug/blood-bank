# Банк крові V6.0.4 Login Feedback Fix

Фінальний фікс після повного аудиту:
- GET /login працює
- login/logout сторінка має явний marker "Вхід у систему"
- неправильний логін повертає сторінку входу без 500
- PWA cache v6.0.4
