Build: pip install -r requirements.txt
Start: gunicorn app:app
ENV: SECRET_KEY, COOKIE_SECURE=1, REQUIRE_HTTPS=1, DEBUG=0, optional DB_PATH=/var/data/blood_bank_v2.db
First login: Sepsis / 1986, then change password.
